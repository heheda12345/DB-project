create database test;
use database test;
create table tbclass(id int, class varchar);
show tables;
create table tbtest(id int not null, name varchar, class int, score float not null, birthday date, primary key(id), foreign key(class) references tbclass(class));
show tables;
drop table tbtest;